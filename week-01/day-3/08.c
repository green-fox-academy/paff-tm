#include <stdio.h>

int main() {
	int h1 = 350;
	int h2 = 200;

	if (h2 * 2 > h1){   // tell if the double of h2 is bigger than h1
        printf("h2 ketszerese nagyobb mint h1 \n");
    }
	return 0;
}
 tell if the double of h2 is bigger than h1